# A+B - add two 10-digit numbers
# Input: "1234567890+9876543210"  (10 digits + '+' + 10 digits)
# Output: "11111111100" (sum)

# Read first number (10 digits) into mem[0-9]
[0]<I
[1]<I
[2]<I
[3]<I
[4]<I
[5]<I
[6]<I
[7]<I
[8]<I
[9]<I

# Read '+' sign (ignore)
A<I

# Read second number (10 digits) into mem[10-19]
[10]<I
[11]<I
[12]<I
[13]<I
[14]<I
[15]<I
[16]<I
[17]<I
[18]<I
[19]<I

# Now add digit by digit from right to left with carry
# Numbers are stored left-to-right,so we start from position 9+19

# Convert ASCII to numeric: subtract 48
# Add: result[i] = (num1[i]-48) + (num2[i+10]-48) + carry
# If result >= 10: carry=1, result-=10
# Convert back to ASCII: add 48

# Use mem[20-30] for result
# Use mem[31] for carry

[31]<0  # Initialize carry to 0

# Add rightmost digit (position 9)
A<[9]
A<A-48  # Can't subtract! Need lookup table

# Create digit value lookup: mem[48]='0'->0, mem[49]='1'->1, etc.
# Wait, can't do arithmetic...

# This needs careful handling
# Let me create addition lookup tables

# For now, let's create a complete addition table
# mem[100 + digit1*10 + digit2] = (digit1 + digit2) % 10
# mem[200 + digit1*10 + digit2] = (digit1 + digit2) / 10 (carry)

# This is very large...

# Alternative: Since we only have mov, addition is hard
# Need to build addition tables for each digit pair
# This will require significant memory

# Focus on core logic structure for now

Z<1
